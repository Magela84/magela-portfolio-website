{
  "name": "azure-cost-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Azure Cost Visibility Dashboard - Express backend + React frontend",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "mock": "cross-env MOCK_DATA=true npm --prefix backend run dev",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "cross-env": "^7.0.3"
  }
}
